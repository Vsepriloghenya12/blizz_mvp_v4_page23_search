# Page 32.2 — Visual pass: Главная / Лента

Дата: 2026-06-14

## Что изменено

- Визуальный проход применён к `mobile/src/screens/home/HomeScreen.tsx` и нижней навигации `mobile/src/navigation/MainTabs.tsx`.
- Структура Главной сохранена по текущему приложению: шапка → строка Близзов → переключатель `Лента / Витрина` → контент.
- Переключатель `Лента / Витрина` размещён под Близзами, как было уточнено в визуальном обсуждении.
- Цветовая гамма сделана глубже и насыщеннее через обновление `colors.primary` в `mobile/src/shared/ui/theme.ts`.
- Убрана лишняя подпись секции `Близзы`, строка стала ближе к референсу.
- Кружки Близзов используют avatar или первый image-story как превью, при отсутствии медиа — fallback с буквой.
- Карточка поста визуально приближена к референсу: более крупный автор, заметная геометка, округлое фото, действия и сохранение в одной нижней строке.
- Нижняя навигация усилена: более насыщенный активный цвет, крупнее центральный `+`, визуально ближе к референсу.

## Что не изменялось

- Backend/API не менялись.
- Логика ленты, витрины, Близзов, комментариев, share и сохранений не менялась.
- `Сообщения` не возвращались в нижнее меню.
- `Близз` не возвращался в `+ / Что создать?`.
- Кнопка `Обновить` не добавлялась.
- `user.isBusiness` не добавлялся.

## Проверки

- `server npm run check` — пройдено.
- `mobile npm install` — пройдено, есть 10 moderate vulnerabilities в Expo dependency tree.
- `mobile npm run typecheck` — пройдено.
- `npx expo export --platform web` — пройдено.
- grep запрещённых элементов — пройдено.
- `unzip -t` — выполняется на финальном архиве.

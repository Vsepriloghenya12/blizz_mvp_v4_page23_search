# Проверка версии 12 — Главная: Лента и Витрина

## Проверено

- `server npm run check`
- `server/page12-smoke-test.js`
- `mobile npm install`
- `mobile npm run typecheck`
- `npx expo export --platform web`
- grep-проверки исходников
- `unzip -t` архива

## Проверенный backend-сценарий

1. Регистрация пользователя.
2. Создание личного поста.
3. Создание бизнес-аккаунта.
4. Создание бизнес-поста.
5. Создание предложения.
6. Проверка, что `Лента` получает личные посты и не получает бизнес-посты.
7. Проверка, что `Витрина` получает бизнес-посты и предложения.
8. Проверка `GET /api/offers/my`.
9. Проверка `GET /api/business/:accountId/offers`.
10. Проверка сохранения предложения.
11. Проверка запрета создания предложения личным аккаунтом.
12. Проверка запрета создания предложения ролью `business messages`.

## Проверенные запреты

- `user.isBusiness` отсутствует.
- Нижней вкладки `Сообщения` нет.
- Кнопки `Обновить` нет.
- Блока `Предложения рядом` нет.
- `Близз` не вернулся как create-action в `CreateHubScreen`.
- Отдельной вкладки `Витрина` в нижнем меню нет.

## Не проверялось интерактивно

Интерактивный запуск в браузере/эмуляторе не выполнялся в sandbox. Проверка через Expo web export прошла.
